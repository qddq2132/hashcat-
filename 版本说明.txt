1.15版本（懒得去做视频）
增加了直接运行脚本可手动输入hash值。用来破解类似于MD5，ntlm密码。
添加了一个小彩蛋在字典文件夹里。

1.1版本
目前脚本只支持5种压缩包格式。文档格式（docx，doc，pdf，xlsx）正在测试中。
新增字典破解模式。
新增hash自动判断。
删除了对于pkzip仅储存压缩包格式的支持。
修复已知bug

1.0版本
hashcat整合包横空出世
